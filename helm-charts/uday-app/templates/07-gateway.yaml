apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: uday-gateway
  namespace: {{ .Values.namespace }}
spec:
  gatewayClassName: istio
  listeners:
    - name: http
      protocol: HTTP
      port: 80
